server:
  port: 8081

app:
  account-service:
    host: localhost
    port: 8082
  expense-service:
    host: localhost
    port: 8083

---
server:
  port: 8080

spring:
  config:
    activate:
      on-profile: docker

app:
  account-service:
    host: account
    port: 8080
  expense-service:
    host: expense
    port: 8080
