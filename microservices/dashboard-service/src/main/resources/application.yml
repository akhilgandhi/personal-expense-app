spring.config.import: "configserver:"

spring:
  application.name: dashboard
  cloud.config:
    failFast: true
    retry:
      initialInterval: 3000
      multiplier: 1.3
      maxInterval: 10000
      maxAttempts: 20