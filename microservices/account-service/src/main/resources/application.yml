server:
  port: 8082

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: account-db

logging:
  level:
    root: INFO
    com.akhil.microservices: DEBUG
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG

---
server:
  port: 8080

spring:
  config:
    activate:
      on-profile: docker
  data:
    mongodb:
      host: mongodb